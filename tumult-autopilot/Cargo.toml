[package]
name = "tumult-autopilot"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Pure decision logic for the Tumult autopilot: policy, anti-hollow validator, deterministic safety gate, earned-autonomy ladder"

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
# Policy-hash only (sha256 of the raw policy text). Not a workspace dep;
# pinned to the 0.10 line already resolved in the lockfile via tumult-cloud.
sha2 = "0.10"

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
