[package]
name = "tumult-authoring"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Authoring ergonomics for Tumult: fault catalog, experiment builder, and curated starter templates"

[lib]
name = "tumult_authoring"
path = "src/lib.rs"

[dependencies]
tumult-core = { path = "../tumult-core" }
tumult-plugin = { path = "../tumult-plugin" }
serde = { workspace = true }
serde_json = { workspace = true }
toon-format = { workspace = true }
indexmap = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
