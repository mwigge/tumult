[package]
name = "tumult-agent-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Adapter layer for invoking agentic coding CLIs (Claude Code, Codex) non-interactively"

[dependencies]
# One-shot subprocess execution is intentionally std::process (blocking) — no tokio.
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
